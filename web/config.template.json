{
  "apiBaseUrl": "${API_BASE_URL:-http://localhost:8080}",
  "apiTimeout": "${API_TIMEOUT:-30000}",
  "enableDebugMode": "${DEBUG_MODE:-false}",
  "enableClusterMode": "${CLUSTER_MODE:-true}",
  "language": "${LANGUAGE:-en}"
} 
